<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    SEO META TAGS                                     -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <title><%= title %></title>
    <meta name="description" content="PULSE — Real-time DAO intelligence engine. Monitor Web3 community health, track contributors, analyze activity patterns, and generate AI-powered insights. Built for decentralized ecosystems.">
    <meta name="keywords" content="DAO, Web3, blockchain, health monitoring, contributor tracking, bounty analytics, decentralization, crypto, smart contracts, decentralized governance">
    <meta name="author" content="PULSE">
    <meta name="robots" content="index, follow">
    <meta name="theme-color" content="#06b6d4">
    <link rel="canonical" href="https://pulse-crvb.onrender.com">
    
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    OPEN GRAPH / SOCIAL SHARING                      -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://pulse-crvb.onrender.com">
    <meta property="og:title" content="PULSE — Real-Time DAO Intelligence Engine">
    <meta property="og:description" content="Monitor DAO health, track contributors, analyze activity patterns, and generate AI-powered insights. The first real-time intelligence platform that doesn't lie.">
    <meta property="og:image" content="https://pulse-crvb.onrender.com/assets/logo.png">
    <meta property="og:image:width" content="512">
    <meta property="og:image:height" content="512">
    <meta property="og:image:alt" content="PULSE — Real-Time DAO Intelligence Engine">
    <meta property="og:site_name" content="PULSE">
    <meta property="og:locale" content="en_US">
    
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    TWITTER CARD                                       -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@pulsewebb3">
    <meta name="twitter:creator" content="@pulsewebb3">
    <meta name="twitter:title" content="PULSE — Real-Time DAO Intelligence Engine">
    <meta name="twitter:description" content="Monitor DAO health, track contributors, analyze activity patterns, and generate AI-powered insights. The first real-time intelligence platform that doesn't lie.">
    <meta name="twitter:image" content="https://pulse-crvb.onrender.com/assets/logo.png">
    <meta name="twitter:image:alt" content="PULSE — Real-Time DAO Intelligence Engine">
    
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    FAVICON & ICONS                                   -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <link rel="icon" type="image/png" href="/assets/logo.png">
    <link rel="apple-touch-icon" href="/assets/logo.png">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    FONTS & STYLES                                    -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    STRUCTURED DATA (JSON-LD)                         -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebApplication",
        "name": "PULSE",
        "description": "Real-time DAO health intelligence engine for Web3 communities",
        "url": "https://pulse-crvb.onrender.com",
        "logo": "https://pulse-crvb.onrender.com/assets/logo.png",
        "applicationCategory": "FinanceApplication",
        "operatingSystem": "Web Browser",
        "offers": {
            "@type": "Offer",
            "price": "0",
            "priceCurrency": "USD"
        },
        "creator": {
            "@type": "Organization",
            "name": "PULSE",
            "url": "https://pulse-crvb.onrender.com"
        },
        "featureList": [
            "Real-time DAO health monitoring",
            "Contributor activity tracking",
            "Bounty lifecycle analytics",
            "AI-powered strategic insights",
            "Activity heatmaps",
            "DAO Scanner"
        ]
    }
    </script>
</head>
<body class="aura-bg">
    <!-- Skip Link for Accessibility -->
    <a href="#main-content" class="skip-link">Skip to main content</a>

    <!-- NOISE OVERLAY -->
    <div class="noise-overlay" aria-hidden="true"></div>

    <!-- STICKY NAV -->
    <header class="sticky-nav" role="navigation" aria-label="Main navigation">
        <div class="container">
            <div class="nav-content">
                <a href="/" class="nav-logo" aria-label="PULSE Home">
                    <img src="/assets/logo.png" alt="PULSE" width="120" height="42">
                </a>

                <nav class="center-nav" aria-label="Desktop navigation">
                    <% if (user) { %>
                        <a href="/dashboard">Dashboard</a>
                        <a href="/intelligence">Intelligence</a>
                        <a href="/scanner">Scanner</a>
                        <a href="/insights">Insights</a>
                        <a href="/profile">Profile</a>
                        <a href="/docs">Docs</a>
                    <% } else { %>
                        <a href="/">Home</a>
                        <a href="/about">Protocol</a>
                        <a href="/features">Features</a>
                        <a href="/auth/login">Sign In</a>
                    <% } %>
                </nav>

                <div class="nav-actions">
                    <% if (user) { %>
                        <% if (user.isWalletAuth && user.walletAddress) { %>
                            <div class="wallet-badge desktop-only" style="margin-right: 1rem; padding: 0.35rem 0.75rem; background: var(--bg-card); border: 1px solid var(--accent-cyan); border-radius: var(--radius-md);">
                                <span class="mono text-cyan" style="font-size: 0.7rem;"><%= user.walletAddress.substring(0, 6) %>...<%= user.walletAddress.substring(user.walletAddress.length - 4) %></span>
                            </div>
                        <% } else if (user.username) { %>
                            <span class="desktop-only mono text-soft" style="margin-right: 1rem; font-size: 0.8rem;"><%= user.username %></span>
                        <% } %>
                        <a href="/auth/logout" class="btn btn-sm btn-secondary desktop-only">Logout</a>
                    <% } else { %>
                        <a href="/auth/register" class="btn btn-sm btn-primary desktop-only">Get Started</a>
                    <% } %>

                    <!-- MOBILE TOGGLE -->
                    <button class="mobile-toggle mobile-only" id="mobile-toggle" aria-label="Toggle mobile menu" aria-expanded="false" aria-controls="mobile-menu">
                        <span></span>
                        <span></span>
                        <span></span>
                    </button>
                </div>
            </div>
        </div>
    </header>

    <!-- MOBILE MENU OVERLAY -->
    <div class="mobile-menu" id="mobile-menu" aria-hidden="true">
        <div class="mobile-menu-header">
            <a href="/" class="nav-logo" aria-label="PULSE Home">
                <img src="/assets/logo.png" alt="PULSE">
            </a>
            <button class="mobile-close" id="mobile-close" aria-label="Close menu">&times;</button>
        </div>
        <nav class="mobile-nav-links" aria-label="Mobile navigation">
            <% if (user) { %>
                <% if (user.walletAddress) { %>
                    <div class="mobile-wallet-display" style="padding: 0.75rem 1rem; background: var(--bg-card); border: 1px solid var(--accent-cyan); border-radius: var(--radius-md); margin: 0.5rem 1rem;">
                        <span class="mono text-cyan" style="font-size: 0.7rem;"><%= user.walletAddress.substring(0, 6) %>...<%= user.walletAddress.substring(user.walletAddress.length - 4) %></span>
                    </div>
                <% } %>
                <a href="/dashboard" class="mobile-nav-item">Dashboard</a>
                <a href="/intelligence" class="mobile-nav-item">Intelligence</a>
                <a href="/scanner" class="mobile-nav-item">Scanner</a>
                <a href="/insights" class="mobile-nav-item">Insights</a>
                <a href="/profile" class="mobile-nav-item">Profile</a>
                <a href="/command-center" class="mobile-nav-item">Command Center</a>
                <a href="/docs" class="mobile-nav-item">Docs</a>
                <div class="nav-divider"></div>
                <a href="/auth/logout" class="mobile-nav-item" style="color: var(--accent-critical);">Disconnect</a>
            <% } else { %>
                <a href="/" class="mobile-nav-item">Home</a>
                <a href="/about" class="mobile-nav-item">Protocol</a>
                <a href="/features" class="mobile-nav-item">Features</a>
                <a href="/auth/login" class="mobile-nav-item">Sign In</a>
                <div class="nav-divider"></div>
                <a href="/auth/register" class="mobile-nav-item" style="color: var(--accent-cyan);">Get Started</a>
            <% } %>
        </nav>
        <div class="mobile-menu-footer">
            <div class="mono text-muted" style="font-size: 0.65rem; text-align: center;">
                PULSE // SYSTEM ONLINE
            </div>
        </div>
    </div>

    <!-- Mobile menu backdrop -->
    <div class="mobile-backdrop" id="mobile-backdrop" aria-hidden="true"></div>

    <!-- Stacks Wallet Provider Detection -->
    <script>
        (function() {
            // Stacks wallets inject these global objects:
            // - window.HiroWallet (Hiro Wallet)
            // - window.leather (Leather Wallet)
            // - window.xverse (Xverse Wallet)

            // Make wallet detection available globally
            window.getStacksWallet = function() {
                if (window.HiroWallet && typeof window.HiroWallet === 'object') {
                    return { type: 'hiro', wallet: window.HiroWallet };
                }
                if (window.leather && typeof window.leather === 'object') {
                    return { type: 'leather', wallet: window.leather };
                }
                if (window.xverse && typeof window.xverse === 'object') {
                    return { type: 'xverse', wallet: window.xverse };
                }
                return null;
            };
        })();
    </script>

    <script>
        // Mobile Menu Functionality
        (function() {
            const toggle = document.getElementById('mobile-toggle');
            const menu = document.getElementById('mobile-menu');
            const closeBtn = document.getElementById('mobile-close');
            const backdrop = document.getElementById('mobile-backdrop');

            function openMenu() {
                menu.classList.add('active');
                menu.setAttribute('aria-hidden', 'false');
                toggle.classList.add('active');
                toggle.setAttribute('aria-expanded', 'true');
                if (backdrop) backdrop.classList.add('active');
                document.body.style.overflow = 'hidden';
            }

            function closeMenu() {
                menu.classList.remove('active');
                menu.setAttribute('aria-hidden', 'true');
                toggle.classList.remove('active');
                toggle.setAttribute('aria-expanded', 'false');
                if (backdrop) backdrop.classList.remove('active');
                document.body.style.overflow = '';
            }

            if (toggle) {
                toggle.addEventListener('click', () => {
                    if (menu.classList.contains('active')) {
                        closeMenu();
                    } else {
                        openMenu();
                    }
                });
            }

            if (closeBtn) {
                closeBtn.addEventListener('click', closeMenu);
            }

            if (backdrop) {
                backdrop.addEventListener('click', closeMenu);
            }

            // Close menu on link click
            document.querySelectorAll('.mobile-nav-links a').forEach(link => {
                link.addEventListener('click', closeMenu);
            });

            // Close on escape key
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && menu.classList.contains('active')) {
                    closeMenu();
                }
            });

            // Set aria-current for active page
            (function() {
                const currentPath = window.location.pathname;
                const centerNav = document.querySelector('.center-nav');
                if (centerNav) {
                    const links = centerNav.querySelectorAll('a');
                    links.forEach(link => {
                        const href = link.getAttribute('href');
                        if (href === currentPath || (currentPath === '/' && href === '/') || (currentPath.startsWith(href) && href !== '/')) {
                            link.setAttribute('aria-current', 'page');
                            link.classList.add('active');
                        }
                    });
                }
            })();
        })();
    </script>

    <main id="main-content">
        <% if(success_msg != '') { %>
            <div class="container" style="margin-top: 2rem;">
                <div class="glass-panel alert-success">
                    <div class="text-healthy mono"><%= success_msg %></div>
                </div>
            </div>
        <% } %>
        <% if(error_msg != '') { %>
            <div class="container" style="margin-top: 2rem;">
                <div class="glass-panel alert-error">
                    <div class="text-critical mono"><%= error_msg %></div>
                </div>
            </div>
        <% } %>
        
        <%- body %>
    </main>

    <footer class="site-footer" role="contentinfo">
        <div class="container">
            <div class="responsive-grid">
                <div class="footer-brand">
                    <a href="/" class="nav-logo" aria-label="PULSE Home">
                        <img src="/assets/logo.png" alt="PULSE">
                    </a>
                    <p class="text-muted mono">VITALITY_INT_LAYER_V3.3</p>
                    <div class="footer-social">
                        <a href="https://x.com/pulsewebb3" aria-label="Twitter" target="_blank" rel="noopener noreferrer">X</a>
                        <a href="https://github.com" aria-label="GitHub" target="_blank" rel="noopener noreferrer">GH</a>
                        <a href="https://discord.com" aria-label="Discord" target="_blank" rel="noopener noreferrer">DC</a>
                    </div>
                </div>
                <div class="footer-links-group">
                    <h4>Platform</h4>
                    <a href="/dashboard">DASHBOARD</a>
                    <a href="/intelligence">INT_REPORT</a>
                    <a href="/scanner">SCANNER</a>
                    <a href="/insights">INSIGHTS</a>
                    <a href="/profile">PROFILE</a>
                </div>
                <div class="footer-links-group">
                    <h4>Resources</h4>
                    <a href="/docs">PROTOCOL_DOCS</a>
                    <a href="/about">ABOUT</a>
                </div>
                <div class="footer-links-group">
                    <h4>Legal</h4>
                    <a href="/privacy">PRIVACY</a>
                    <a href="/terms">TERMS</a>
                </div>
                <div class="footer-copy">
                    <div class="mono text-muted">© 2026 PULSE // SYSTEM_ONLINE</div>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>
